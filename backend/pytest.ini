[pytest]
; Anchors rootdir here regardless of invocation cwd, and puts backend/ on
; sys.path so `from agent.x import ...` / `from services.x import ...`
; style imports resolve the same way whether pytest is invoked as
; `pytest backend/tests` from the repo root or `pytest` from inside backend/.
pythonpath = .
