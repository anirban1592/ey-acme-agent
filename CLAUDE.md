# CLAUDE.md

> This file is updated continuously as the project evolves. Treat it as the source of truth over any stale summary in `project.md`. The user maintains this file directly and will keep appending to it — Claude should propose additions here whenever a new architectural decision, convention, or gotcha is established.

## Project Summary

An AI assistant for tracking customer issues and account management. Users log in via Keycloak, chat with an assistant (React frontend) that talks to a FastAPI backend, which will eventually orchestrate an agentic layer to query/update issue data. Full context: `project.md`. Current phase/subphase status: `progress-tracker.md`.

## Repo Layout

_(Update this section as the repo grows — keep it accurate, not aspirational.)_

```
stage2-project/
├── backend/              # FastAPI app
│   ├── evals/               # DeepEval golden-question eval suite (tool selection + response correctness)
│   └── tests/               # pytest unit tests (response_types, auth, models) — run via `pytest backend/tests`
├── frontend/              # React chat interface
│   └── src/
│       ├── components/      # Header, Sidebar, ChatWindow, WaitingIndicator, ResponseRouter + typed response renderers
│       ├── styles/theme.css   # dark-theme CSS custom properties (no CSS framework dependency)
│       └── conversationStore.ts  # client-side (localStorage) multi-thread chat history
├── keycloak/               # realm-export.json and related Keycloak bootstrap config
├── docker-compose.yml      # brings up the full local stack
├── .env.example             # documents required environment variables
├── README.md                 # setup & run instructions
├── project.md                 # stable project brief
├── CLAUDE.md                   # this file
└── progress-tracker.md          # phase/subphase tracker
```

## Conventions

_(Fill in as decisions are made — placeholders below.)_

- **Backend code style**: TBD (e.g., black/ruff formatting, type hints required).
- **API conventions**: TBD (e.g., REST resource naming, error response shape, versioning).
- **Frontend code style**: TBD (e.g., ESLint/Prettier config, component structure).
- **Commit conventions**: TBD (e.g., Conventional Commits).
- **Environment variables**: always add new ones to `.env.example` with a comment, never commit real secrets.

## Key Architectural Decisions Log

| Date | Decision |
|---|---|
| 2026-07-16 | Backend will be Python + FastAPI (chosen for future agentic/AI orchestration fit). |
| 2026-07-16 | Keycloak realm/clients/sample users are bootstrapped via a mounted realm-import JSON (`--import-realm`) — fully declarative, no manual admin-console setup. |
| 2026-07-16 | Postgres and the app data model are explicitly deferred to Phase 2 — Phase 1 backend has no database dependency; Keycloak runs in dev mode with its embedded store. |
| 2026-07-16 | Repo uses plain folders (`backend/`, `frontend/`) with independent dependency manifests — no monorepo workspace tooling (npm/pnpm workspaces, Nx, Turborepo) until there's shared code that justifies it. |
| 2026-07-18 | Phase 3 redefined: no standalone backend service-layer module. Instead, a new top-level `mcp/` directory (independent dependency manifest, `fastmcp` package) talks to Postgres directly via its own `asyncpg` connection pool, created once at startup through FastMCP's `lifespan` hook and reused across tool calls. `mcp/db.py`'s query functions are the service layer for now. The MCP-server work originally filed under Phase 4 (wrapping a Phase 3 service layer) moves to Phase 3 itself — see `progress-tracker.md`. |
| 2026-07-18 | `issues.domain` (added nullable/unused in Phase 2.4) is repurposed to encode which role can see an issue, for the MCP server's `retrieve_customer_profile` tool: filtered as `issues.domain = role` unless `role == 'admin'`, in which case the filter is omitted entirely. |
| 2026-07-18 | Phase 4.1 (backend DB connection + user-validation gate): `users.keycloak_id` will be populated deterministically by pinning explicit `"id"` UUIDs for the seed users in `keycloak/realm-export.json` and mirroring them in `postgres/init.sql`'s seed data — not via JIT/runtime provisioning — since Keycloak otherwise assigns a random UUID per user on every realm import, leaving nothing static in Postgres to match a JWT's `sub` claim against. |
| 2026-07-18 | Backend DB access uses raw `asyncpg` (a pool created once at startup via FastAPI's `lifespan`), mirroring `mcp/db.py`'s pattern rather than introducing an ORM — keeps `backend/` and `mcp/` consistent; query functions remain the service layer for now, same convention as Phase 3. |
| 2026-07-18 | The DB-backed user-validation gate (`get_current_valid_user`, Phase 4.1) is additive alongside the existing JWT-only `get_current_user`/`/me` — not a replacement — so future agent/chat endpoints opt into the stricter check without changing existing Phase 1 behavior. |
| 2026-07-18 | For automated testing (password grant), Keycloak's `frontend-spa` client is updated with `directAccessGrantsEnabled=true`, and realm requiredActions are explicitly imported with `defaultAction=false` to prevent Keycloak from blocking login requests with required setup gates. |
| 2026-07-18 | Fixed a bug in `backend/auth.py` where `jwt.construct_rsa_key` was called, which does not exist in `python-jose`; replaced with `jwk.construct(key_dict)` to correctly decode RSA signatures. |
| 2026-07-18 | JWT verification is updated to bypass audience check (`options={"verify_aud": False}`) because standard direct access tokens issued by Keycloak do not populate the `aud` claim by default, and `KEYCLOAK_ISSUER` is set to `http://localhost:8080/realms/assistant` in the backend service configuration. |
| 2026-07-18 | Phase 4.2 (agent over WebSocket): a new `backend/agent/` directory is the home for the agent construct going forward (tools, workflows, sub-agents attach here in later subphases) — kept separate from `backend/main.py`'s routing/auth concerns. |
| 2026-07-18 | Phase 4.2: the agent is built with LangChain v1's unified `create_agent(model, tools, system_prompt)` (`langchain.agents`, backed by LangGraph), not a hand-rolled `ChatOpenAI` chain or the older `AgentExecutor`/`create_react_agent` style — chosen specifically because `create_agent` takes tools/system_prompt/sub-agent-composition as first-class constructor args, matching the project's need to attach MCP tools (4.3) and prompt design (later) without restructuring the agent later. Requires `langchain>=1.0`. |
| 2026-07-18 | Phase 4.2: the `/ws/chat` WebSocket endpoint authenticates via a `token` query param at connect time (not an `Authorization` header, which WebSocket handshakes can't carry), reusing 4.1's `verify_jwt` + `UserService.validate_user` checks; the socket is rejected with close code `1008` if either check fails. Wire protocol is a JSON envelope (`{"message": ...}` in, `{"reply": ...}` out) rather than plain text, so later subphases can add metadata without changing the message shape. |
| 2026-07-18 | Sourced `OPENAI_API_KEY` in `docker-compose.yml` backend service environment, and implemented fallback key `"mock-key"` in `backend/agent/core.py` to prevent import-time crashes and allow container health checks to succeed when no real key is set. |
| 2026-07-18 | Updated `verify_jwt` in `backend/auth.py` to dynamically whitelist both `localhost:8080` (host-to-container) and `keycloak:8080` (container-to-container) token issuer claims to support testing inside docker and runtime chat outside docker. |
| 2026-07-18 | Phase 4's scope now explicitly includes end-to-end UI integration through the auth layer (login → JWT → WebSocket → agent → response), not just backend agent work — Phase 5's originally-listed "WebSocket-based chat UI integration" moved into Phase 4.3, so Phase 5 is now escalation workflow + RBAC refinement only. |
| 2026-07-18 | Phase 4.3's chat UI reuses the existing Keycloak JS adapter's `onLoad: 'login-required'` gate (`frontend/src/App.tsx`, Phase 1.4) as the protected-page mechanism — no router or new page added, since the app stays a single page and unauthenticated visits already redirect to Keycloak login before anything renders. |
| 2026-07-18 | Phase 4.3: `backend/agent/core.py`'s `create_agent(...)` call moved from module import time (where it was hardcoded to `tools=[]`) to a lazy, lock-guarded `get_agent()` built on first call to `respond()` — module-level construction can't `await` the async `MultiServerMCPClient.get_tools()` fetch, so the MCP tools were never actually reaching the agent. The MCP client URL is now `MCP_SERVER_URL` (env var, defaults to `http://mcp:9000/mcp`, the docker-compose service name) instead of a hardcoded `http://localhost:9000`, which would not resolve from inside the `backend` container to the separate `mcp` container. |
| 2026-07-18 | `MCP_SERVER_URL` must include the `/mcp` path suffix (`http://mcp:9000/mcp`), not just the host:port — FastMCP's `http` transport serves the MCP protocol endpoint at `/mcp`, not `/`. Pointing `MultiServerMCPClient` at the bare host:port caused every request to 404, which surfaced as a confusing client-side `anyio` TaskGroup error (`McpError: Session terminated`) rather than a clear 404 — the chat UI only showed `"Error: unhandled errors in a TaskGroup (1 sub-exception)"` because `main.py`'s WebSocket handler's catch-all `except Exception as e: str(e)` collapses the ExceptionGroup instead of unwrapping the real sub-exception. |
| 2026-07-18 | Phase 4.5 (RBAC-enforced MCP tool calls): role-scoping for `retrieve_customer_profile` is now driven entirely by the authenticated `User` from `backend/main.py`'s `/ws/chat` handler, never by whatever the LLM puts in its own tool-call arguments — closes the risk flagged in Phase 3 ("`role` trusted as a plain string from the caller"). Mechanism (verified against current LangChain v1 / `langchain-mcp-adapters` docs, not assumed): `create_agent(..., context_schema=AgentContext)` + `agent.ainvoke(..., context=AgentContext(username=..., roles=...))` passes per-invocation, non-LLM-controlled data into the existing shared/cached `_agent` singleton without rebuilding it; a `@dynamic_prompt`-decorated middleware function (`langchain.agents.middleware`) builds the system prompt per call from `request.runtime.context`, replacing the old static `SYSTEM_PROMPT`; `MultiServerMCPClient(..., tool_interceptors=[inject_role_interceptor])` (`langchain_mcp_adapters.interceptors.MCPToolCallRequest`) unconditionally overwrites the `roles` tool-call argument with the authenticated context's roles via `request.override(...)` before the call reaches the MCP server — this interceptor is the actual security boundary, not the prompt. `langchain-mcp-adapters` pinned to `>=0.3.0` in `backend/requirements.txt` (confirmed installed version exposes this API). |
| 2026-07-18 | `retrieve_customer_profile`'s `role: str` argument became `roles: list[str]` (`mcp/server.py`, `mcp/db.py`) — a user can hold multiple roles via the `user_roles` join table, so the single-string collapse was dropped in favor of full multi-role support: admin bypass triggers if `"admin"` is anywhere in `roles`; otherwise `WHERE i.domain ILIKE ANY($2::text[])`; an empty `roles` list fails closed (matches nothing, no special-casing needed). The MCP server's trust model is otherwise unchanged — it still trusts `roles` as given by the caller (no JWT validation inside `mcp/`); the backend remains the trusted caller per Phase 3's existing decision. |
| 2026-07-19 | Phase 4.6: added the codebase's first sub-agent (`backend/agent/customer_profile.py`), using a manual "agent-as-tool" pattern — a plain `@tool`-wrapped `create_agent(...)` sub-agent invoked via a wrapper tool (`consult_customer_profile_agent`) — instead of the `deepagents` package's `SubAgentMiddleware`, to avoid a new dependency for a single-tool sub-agent. It owns its own `ChatOpenAI` instance rather than importing `core.py`'s, both to avoid a `core.py`/`customer_profile.py` circular import and to match the existing per-module config-ownership convention (`mcp/db.py` duplicating `backend/db.py`'s DSN/pool logic). Also fills in `backend/services/customer_service.py` (previously an empty Phase-4-reserved stub) as a deliberate, scoped exception to the Phase 3 "MCP-only DB access" convention: it runs its own SQL directly against `customer_details` (CRM/account data — industry, account tier, contract value, risk level, sentiment, notes) via its own lazily-created `asyncpg` pool, independent of `backend/db.py`'s `app.state.db_pool`. No RBAC interceptor changes were needed because `customer_details` has no role/domain column, unlike `issues` — it is not role-scoped data. |
| 2026-07-19 | Phase 4.7: `issues.domain` column removed entirely — `postgres/initv2.sql`'s seed data set `persona` on every issue but never set `domain`, so the Phase 4.5/2026-07-18 domain-based RBAC filter was matching nothing for any non-admin role in practice. RBAC for `retrieve_customer_profile` and the new `retrieve_issue_updates` tool is now based directly on `issues.persona` matching the caller's role name (`ILIKE ANY`, unchanged admin-bypass-if-`"admin"`-is-anywhere-in-`roles` semantics) — this supersedes the prior domain-based decision, not just a rename, since seeded users (`bob`→sales, `charlie`→operations, `tony`→support, `alice`→admin) map 1:1 onto `persona` values. New tool `retrieve_issue_updates(issue_id, roles)` (`mcp/server.py`, `mcp/db.py`, `mcp/models.py`'s new `IssueUpdate`/`IssueWithUpdates`) returns an issue plus its `issue_updates` rows newest-first, scoped the same way; `backend/agent/core.py`'s `inject_role_interceptor` tool-name check was extended to a tuple covering both tools, since that interceptor — not the MCP server or the prompt — is the actual RBAC security boundary. |
| 2026-07-19 | Phase 4.8: extracted `inject_role_interceptor` and `get_mcp_tools()` out of `backend/agent/core.py` into a new standalone `backend/agent/mcp_client.py` (no dependency on `core.py` or any sub-agent module), so every agent graph that talks to the MCP server — the main agent and the new summarizer sub-agent alike — shares the exact same RBAC enforcement point instead of each duplicating it; unlike the Phase 4.6 precedent of each sub-agent owning its own independent (benign) config, a security boundary specifically must not be duplicated, since duplicates can drift out of sync as more RBAC'd MCP tools are added. This also avoids a `core.py` ↔ summarizer circular import. Added the codebase's second sub-agent, `backend/agent/summarize_agent/agent.py` (filling in a pre-existing stub; also fixed `summarize_agent/__init__.py`, which was accidentally an empty directory rather than a file), wrapping just the `retrieve_issue_updates` tool (filtered out of the full MCP tool list by name) and exposed via `consult_summarizer_agent`. Because `retrieve_issue_updates` is RBAC'd, the summarizer's `create_agent(...)` must declare `context_schema=AgentContext`, and `consult_summarizer_agent` must explicitly forward the *outer* agent's `AgentContext` (via `runtime: ToolRuntime[AgentContext]`) into the summarizer's own `ainvoke(..., context=...)` call — `inject_role_interceptor` reads whichever agent graph is currently executing's own runtime context, so skipping this forwarding doesn't silently bypass RBAC, it fails loudly instead (verified: both behaviors confirmed against a live MCP server + Postgres + real LLM calls, not mocked). |
| 2026-07-19 | Phase 4.9: added the codebase's third sub-agent, `backend/agent/escalation_agent/` (filling in a pre-existing broken stub), which drafts escalation/notification emails using a "skill" pattern verified against LangChain's own documented convention (`docs.langchain.com/oss/python/langchain/multi-agent/skills` — part of core `langchain`, not the `deepagents` package, consistent with the 4.6/4.8 reasoning to avoid that dependency): a single `load_skill(skill_name: str) -> str` tool whose docstring lists the available skill names/descriptions, so the agent's own tool-call decision is the "pick a skill" step (progressive disclosure — full skill content only enters context once chosen). Two `SKILL.md` files (`skills/internal_email/`, `skills/customer_email/`) each document 3 tones with concrete structural guidance, and the agent picks a tone by reasoning over the loaded content, not via another tool call. Unlike the summarizer, this sub-agent has no `context_schema`/RBAC dependency at all — it never touches protected data, only drafts text from the situational description it's given — so `consult_escalation_agent` needs no `ToolRuntime`/context forwarding. Scope is deliberately draft-only: the tool returns composed email text for a human to review and send; no SMTP/email-sending infrastructure exists in the project (or was requested) — the drafted email is not claimed to have been sent. |
| 2026-07-19 | Phase 4.10: added Redis-backed short-term conversation memory via LangGraph's `AsyncRedisSaver` (`langgraph-checkpoint-redis`, verified against current docs via context7, not assumed). New `backend/agent/checkpointer.py` is a lazy, lock-guarded singleton (`get_checkpointer()`/`close_checkpointer()`) mirroring `core.py`'s `get_agent()` pattern — deliberately **not** threaded through FastAPI `app.state` like `db_pool`, since (unlike the DB pool) no code in `main.py` ever consumes the checkpointer object directly; only `agent/core.py`'s `create_agent(checkpointer=...)` and `respond()`'s `ainvoke(config=...)` do, so keeping it self-contained preserves `respond()`'s existing zero-FastAPI-dependency design and matches the established convention that resources used only inside `backend/agent/` own themselves. `docker-compose.yml` adds a `redis` service on the `redis:8` image specifically (not an alpine variant) because Redis 8.0+ is the only image confirmed to bundle the RedisJSON/RediSearch modules `AsyncRedisSaver.asetup()` requires to build its indices; the service runs on `tmpfs` (no named volume), matching Postgres's existing ephemeral-by-design convention and fitting a feature literally named "short-term memory." `thread_id` is carried in the WebSocket JSON envelope in both directions (not a query param): `backend/main.py`'s `/ws/chat` loop generates `str(uuid.uuid4())` whenever the incoming message omits `thread_id`, and echoes it back on **every** reply (not just the first), so the frontend's logic can stay as simple as "always store whatever comes back" and stays correct across reconnects. `frontend/src/useChatSocket.ts` persists `thread_id` in a plain `document.cookie` (`chat_thread_id`, 30-day expiry, no new dependency) so a page reload resumes the same conversation. |
| 2026-07-19 | Phase 4.11: replaced the main agent's free-text reply with a typed, validated WebSocket contract. New `backend/agent/response_types.py` defines content-only Pydantic models (`IssueListContent`, `CustomerProfileContent`, `EscalationEmailDraft`, `BulletSummaryContent`, `ChatMessageContent`) plus enveloped `*Response` wire types (adding `request_id`/`timestamp`/`role_context`) combined into a `WsResponse` discriminated union (`type` literal); `role_context` extends the originally-requested `sales\|admin\|support` to also include `operations`, matching the real seeded persona values (Phase 4.7) rather than silently misclassifying that role. The normalization mechanism is LangChain v1's native structured output — `create_agent(..., response_format=ToolStrategy(...))` — applied at every layer (`escalation_agent`, `summarize_agent`, `customer_profile.py`'s sub-agent, and the main agent's own union), not hand-rolled message-parsing; **`ToolStrategy(...)` must be used explicitly** rather than passing a bare Pydantic class/Union and relying on auto-selected `ProviderStrategy` — during implementation, `customer_profile.py`'s bare-class `response_format` silently fell back to ordinary prose (`result["structured_response"]` simply absent) while the same bare-class pattern happened to work for the other two sub-agents; explicit `ToolStrategy` fixed it and was then applied everywhere for consistency, since the failure mode is silent, not an error. Envelope fields are never LLM-produced — `respond()` always overwrites/attaches them server-side after `ainvoke()` returns, mirroring the RBAC convention of never trusting the model for security/consistency-critical fields. `escalation_email`'s `to` field follows the same rule at *both* layers: the escalation sub-agent's `response_format` is `EscalationEmailDraft` (subject/body only, no `to` field exists in its schema), and the *main* agent's response_format union also uses `EscalationEmailDraft`, not the `to`-bearing `EscalationEmailContent` — live testing showed that when the main agent was allowed a `to` field in its own structured output, it fabricated a different, more "plausible-looking" address instead of passing through the sub-agent's placeholder verbatim; excluding the field from the schema entirely (rather than just prompting against it) closes that off structurally, and `respond()` attaches the fixed `DUMMY_RECIPIENT_EMAIL` placeholder (`response_types.py`) itself. `ChatMessageContent.message` needed an explicit Pydantic `Field(description=...)` plus a system-prompt clarification after live testing showed the model initially populating it with meta-commentary about its own reasoning ("The user initiated a casual greeting...") instead of an actual reply — both fixes were necessary, not just one. `frontend/package.json` adds `zod` (schemas mirror the Pydantic models field-for-field; `z.infer` derives the TS types so type and runtime guard can't drift apart) and, discovered during this phase, `typescript` was entirely absent as a dependency — `vite build` never type-checked anything (esbuild strips types without validating them), so the user's required "build fails if a switch case is missing" exhaustiveness guarantee was previously impossible; `"build"` is now `"tsc && vite build"` (verified: temporarily removing a case from the new `ResponseRouter.tsx` makes `npm run build` fail on the `never` assertion, confirming this isn't cosmetic). |
| 2026-07-19 | Phase 4.11 follow-up: fixed a user-reported bug where issue-history queries returned an untyped prose paragraph instead of `bullet_summary`. `BASE_SYSTEM_PROMPT` (`backend/agent/core.py`) never specified *which* structured shape to use for which tool-sourced content, so the main agent could legally (if unhelpfully) narrate `retrieve_issue_updates` results as `chat_message` prose instead of `bullet_summary` — fixed with an explicit rule: any tool-sourced structured data must use its matching shape, never `chat_message`. Also found and fixed an independent bug while investigating: `frontend/src/components/ChatMessageBubble.tsx` had no `white-space: pre-wrap`, so even a correctly-typed multi-line `chat_message` would visually collapse into one paragraph in the browser — added the same `pre-wrap` style already used in `EscalationEmailCard.tsx`. |
| 2026-07-20 | Frontend UI overhaul (dark theme, header, multi-thread sidebar, waiting indicator) — pure UI work layered on top of the existing `useChatSocket.ts` contract, which is **not modified**. Past-conversation history is persisted **client-side only** via a new `frontend/src/conversationStore.ts` (`localStorage` key `acme.chat.threads.v1`, capped at 20 threads) — deliberately not a new backend history-fetch endpoint, since the backend's Redis/LangGraph memory (Phase 4.10) is server-side agent context only and is never replayed to the client; only the *visible* transcript needed reconstructing. Because `useChatSocket.ts` seeds its `thread_id` only from the `chat_thread_id` cookie at hook-mount time and exposes no thread-switching API, switching threads / starting a new chat works by writing the target thread id into that same cookie (`conversationStore.ts`'s `setActiveThreadCookie`, reusing the hook's own documented contract) and then forcing the calling component to remount via `key={activeThreadId}` in `Chat.tsx` — UI-layer orchestration, not a hook change. `Chat.tsx` (kept as the file `App.tsx` imports) became the layout orchestrator composing new `Header.tsx`/`Sidebar.tsx`/`ChatWindow.tsx` (the last extracted from `Chat.tsx`'s old transcript+input body); a "New chat" click seeds one client-only synthetic `chat_message` greeting (`Hello <username>, how can I help you today?`) that is never sent over the wire. The waiting/"thinking" indicator (`WaitingIndicator.tsx`, cycling phrases) is derived purely from whether the last message in the combined (stored history + live hook `messages`) list has `role === 'user'` — no hook change needed since `useChatSocket` already exposes `messages`/`role`. Styling is plain CSS (`frontend/src/styles/theme.css`, CSS custom properties) rather than a new framework dependency (no Tailwind/styled-components), consistent with this repo's minimal-dependency convention; typography pairs Fraunces (brand wordmark only) + Inter (UI/body) + IBM Plex Mono (timestamps/metadata), loaded via a Google Fonts `<link>` in `index.html`. |
| 2026-07-20 | LangSmith observability integration: added `LANGSMITH_TRACING`/`LANGSMITH_ENDPOINT`/`LANGSMITH_API_KEY`/`LANGSMITH_PROJECT` to `.env`, but no traces appeared — root cause was `docker-compose.yml`'s `backend` service `environment:` block explicitly whitelists every var it forwards into the container (mirroring `OPENAI_API_KEY=${OPENAI_API_KEY}`), and the four LangSmith vars were never added to that list; Compose only auto-reads the root `.env` for `${VAR}` substitution *within* `docker-compose.yml` itself, it does not auto-inject arbitrary host `.env` keys into a container. Fixed by adding all four to the `backend` service's `environment:` block (same `${VAR}` pattern) and documenting them in `.env.example`. Confirmed this is the only gap: `backend/` has no `python-dotenv`/`load_dotenv()` anywhere, so the container's env is populated solely by `docker-compose.yml`'s `environment:` list — any future env var (LangSmith or otherwise) needs an entry there, not just in `.env`, or it silently never reaches the process (LangSmith's tracing hook fails silent with no error when its env vars are absent). `LANGSMITH_ENDPOINT` must also match the region of the LangSmith org the API key belongs to (`eu.api.smith.langchain.com` vs `api.smith.langchain.com`) — a mismatch there authenticates against the wrong region and traces still won't appear. |
| 2026-07-20 | UI overhaul bugfix: `conversationStore.ts`'s `localStorage` key was a single global `acme.chat.threads.v1`, so every user sharing a browser saw (and could switch into) every other user's threads — reported live (Alice's 5 threads all visible to Bob). Fixed by namespacing the key per user: `loadThreads`/`saveThreads`/`bootstrapThreads` now take a `userKey` parameter and read/write `acme.chat.threads.v1:${userKey}` (`Chat.tsx` passes `username ?? 'anonymous'`). The shared `chat_thread_id` cookie `useChatSocket.ts` owns is still global/unscoped (unchanged, per the "don't touch the hook" constraint), but this is harmless: `bootstrapThreads` already falls back to the current user's own most-recently-updated thread whenever the cookie's thread id isn't found in that user's now-namespaced list, so a stale cookie from a previous user never resolves to a thread the new user doesn't own. `App.tsx` also now keys `<Chat>` by `username` as defense-in-depth, forcing a full remount (and fresh store bootstrap) if the logged-in identity ever changed without a full page navigation. Pre-fix data under the old un-namespaced key is simply orphaned, not migrated. |
| 2026-07-20 | Phase 4.11 follow-up #2: fixed a bug where a turn triggering two tools whose results map to two different structured shapes (e.g. a customer profile and their issue list in the same reply) only ever surfaced one shape — `ToolStrategy(Union[...])` forces the agent's entire final answer into exactly one member of the union, so the model had no way to emit two typed outputs for one turn. Fixed by adding `CompositeContent` (`backend/agent/response_types.py`) — `blocks: list[Union[the 5 existing content types]]` — as one more member of the same `response_format` union; `BASE_SYSTEM_PROMPT` now instructs the agent to use it only when a turn genuinely spans more than one shape, never to wrap a single-shape answer. The "content instance → enveloped `WsResponse`" logic (previously inline in `core.py`'s `respond()`, including the `EscalationEmailDraft`→placeholder-`to` special case) was extracted to `response_types.py`'s `build_ws_response()` so it's reused both for a plain answer and for each block of a composite one. `respond()` now returns `list[WsResponse]` (normally one element) instead of a single `WsResponse`; `backend/main.py`'s `/ws/chat` loop sends one `{"reply": ...}` WebSocket message per element. No frontend change was needed: `useChatSocket.ts` already appends every incoming WS message as its own independent chat bubble, so multiple messages for one turn already render as multiple bubbles. `CompositeContent` is backend-only — it is never itself a wire type, and `WsResponse`/`AgentResponseSchema`/`ResponseRouter` are unchanged. |
| 2026-07-20 | Phase 4.11 follow-up #3: fixed a bug where a narrow follow-up question about already-fetched data (e.g. asking "how many employees are there?" right after the agent showed a customer profile) re-triggered the full `customer_profile` structured shape instead of a short direct answer. Root cause was two-fold: `BASE_SYSTEM_PROMPT`'s rule ("if you called a tool that returned structured data, always use the matching shape, never chat_message") was an unconditional blanket rule with no carve-out for narrow single-fact follow-ups, and nothing told the model it could answer from the Redis-checkpointed thread history (Phase 4.10) instead of re-invoking a tool. Fixed by rescoping that rule to requests to see/view/list the *whole* entity, and adding an explicit rule that a narrower follow-up about one fact within an already-known entity must be answered with `chat_message` containing just the direct answer, and that the model should check conversation history for already-fetched data before calling a tool again. No code/schema change — prompt-only. |
| 2026-07-20 | Phase 5 (early): added a prompt-injection / off-topic guardrail in front of the main agent, implemented as `backend/agent/guardrail.py`'s `PromptInjectionGuardrailMiddleware(AgentMiddleware)` — a code-level `abefore_agent` hook (`@hook_config(can_jump_to=["end"])`), not a prompt-only instruction, mirroring this codebase's existing "security boundaries live in code, not the prompt" convention from the RBAC interceptor decisions (Phase 4.5/4.8). `before_agent`/`abefore_agent` runs once per `agent.ainvoke()` call — i.e. once per WebSocket message, since each is its own `ainvoke()` resuming from the checkpointed thread — strictly before the model or any tool is touched, verified against the actual installed `langchain` package source (not assumed) rather than the docs snippet alone, since the docs' combined `hook_config`+async-hook example used `abefore_model`, not `abefore_agent`; `factory.py` confirmed `abefore_agent`-only overrides (no sync `before_agent` needed, since this app only ever calls `ainvoke()`) are fully supported and wired into the graph correctly. The guardrail classifies only the *latest* human message (not `state["messages"][0]`, since threads are multi-turn/checkpointed) via a small model (`GUARDRAIL_MODEL`, defaults to `openai:gpt-5.4-nano`, matching the existing sub-agent "nano" convention for lightweight auxiliary tasks) bound to a typed `GuardrailVerdict(verdict: Literal["allow","block"], reason: str)` schema via `bind_tools(..., tool_choice="any")` — typed, not string-matched, consistent with this codebase's structured-output-everywhere convention. Blocks prompt-injection attempts and off-domain questions (e.g. "who is the prime minister?") while explicitly allowing small talk/greetings (confirmed with user, matching the existing `chat_message` design for plain conversation, Phase 4.11); the same fixed `REJECTION_MESSAGE` is used for both block reasons deliberately, and `reason` is logged, never surfaced, so a rejected user can't use the response to iteratively probe the classifier. When the guardrail short-circuits via `jump_to="end"`, the graph never reaches the structured-output formatting step, so `result["structured_response"]` comes back `None`; `respond()` (`backend/agent/core.py`) now treats `None` there as an unambiguous signal (every normal completion is required to end in a `structured_response` by construction of `response_format=ToolStrategy(...)`) and returns a plain `ChatMessageResponse` built from the injected rejection message instead of routing through `build_ws_response`. `GUARDRAIL_MODEL` was added to `docker-compose.yml`'s `backend` environment block (same `${VAR:-default}` pattern as `MAIN_AGENT_MODEL`/`CUSTOMER_PROFILE_AGENT_MODEL`/`SUMMARIZE_AGENT_MODEL`) but deliberately not added to `.env.example`, matching the actual (not aspirational) precedent that none of those sibling model vars are documented there either. |
| 2026-07-20 | Guardrail follow-up: fixed a bug where the guardrail (above) blocked legitimate follow-up questions in an already-allowed conversation — e.g. asking "how many employees are there?" right after the assistant showed a customer profile — because it classified only the single latest human message with zero conversation context, so a bare follow-up with no domain keyword read as off-topic. Fixed by collecting the last `MAX_HISTORY_TURNS` (8) human messages from `state["messages"]` and, when there's more than one, formatting prior turns as numbered context ahead of an explicit "Message to classify: ..." line, rather than sending raw alternating chat turns — deliberately not reusing `AIMessage.content` as context, since every final answer in this app is delivered via a `ToolStrategy` tool call, so `AIMessage.content` is always empty and carries no signal; only the human side of the conversation is used. `GUARDRAIL_SYSTEM_PROMPT` was extended with this exact scenario as a worked example and an explicit instruction to allow topic continuations even without a keyword. A guardrail-blocked message still gets classified fresh each turn (nothing about a prior block is cached/remembered), so this only changes what counts as "in context," not whether every message is still checked. |
| 2026-07-20 | Fixed a reported bug where a legitimate mid-conversation question (guardrail already passed, several prior follow-ups already worked) came back as a raw `ErrorResponse(code="agent_error", message="Unrecognized structured_response type: <class 'dict'>")` instead of a normal reply. `backend/agent/response_types.py`'s `build_ws_response()` only ever handled `content` that was already one of the 5 known Pydantic content classes; a plain `dict` (suspected but not confirmed to come from the Redis/LangGraph checkpointer round-tripping a prior turn's `structured_response` through serialization — `factory.py`'s `_handle_model_output` confirms `structured_response` is normally always a validated Pydantic instance, never a raw dict, under a fresh model tool call) fell into the "unrecognized type" branch and raised, which `respond()`'s outer `except Exception` turned into a raw technical `ErrorResponse`. Per the user's explicit request for a catch-all, `build_ws_response` now: (1) tries the existing strict path unchanged; (2) if `content` is a `dict`, attempts one recovery step — re-validating it via a `TypeAdapter` over the 5 leaf content types (deliberately excluding `CompositeContent`, since a block should never itself be a nested composite) — which recovers the *correct* rich shape (table/card) if the dict's fields were actually valid, rather than needlessly degrading a good answer to plain text; (3) only if both fail, logs it and falls back to a plain `ChatMessageResponse` with a best-effort extracted message (a `.message` attribute, or a `"message"/"body"/"heading"` dict key) or a fixed friendly apology — never the raw `repr()` of the failed object. This is applied inside `build_ws_response` itself (not `respond()`'s outer catch), so a single malformed block within a `CompositeContent` answer now degrades only that one block instead of discarding the whole reply. |
| 2026-07-20 | Phase 6.1: added a DeepEval golden-question eval suite (`backend/evals/`) — a hand-drafted set of 19 goldens (question, expected top-level tool(s), expected-response description, seeded username), scored on two metrics: `ToolCorrectnessMetric` (tool selection, matched by name only) and a custom `GEval` "Response Correctness" criterion (there's no predefined metric for domain-specific correctness like RBAC-scoped data or exact guardrail-rejection text). This replaced an earlier untracked, never-committed scaffold that imported `deepeval.evaluate` but never called it and crashed immediately (sync call to the async `respond()` with `user=None`). `agent_runner.py` mirrors `respond()`'s own `agent.ainvoke()` call (rather than calling `respond()` directly) purely to thread a `ToolCallRecorder` callback through and expose tool calls, which `respond()`'s `list[WsResponse]` contract doesn't surface. `test_golden_eval.py` builds every `LLMTestCase` inside one `asyncio.run()` call before any pytest assertion, since the agent singleton's Redis checkpointer and MCP HTTP client bind to whichever event loop first builds them (`get_agent()`) — pytest-asyncio's default fresh-loop-per-test would break every case after the first; this is DeepEval's own documented pattern for pre-built test cases. `.github/workflows/ci.yml` runs the live suite (real paid OpenAI calls, both for the agent and as DeepEval's judge model) only on the nightly cron or manual `workflow_dispatch`, not on every push/PR, split into a separate `golden-eval` job from the existing fast `unit-tests` job. This project's OpenAI org has a tight ~200k TPM limit that a single agent turn (guardrail classifier + main agent + often a sub-agent call + structured-output formatting) can exhaust mid-run; `agent_runner.py` retries on `RateLimitError` with backoff (up to 12 retries, capped ~90s) plus a 15s pause between cases. Full details, including the first live smoke-test result, are in `progress-tracker.md`'s Phase 6.1 entry. |
| 2026-07-21 | Added `backend/tests/` (pytest unit tests for `response_types.py`, `auth.get_key`, and the `models.py` Pydantic models — fast, no live services/network) and fixed CI's `unit-tests` job, which was silently broken: `pytest -q` from the repo root couldn't resolve this codebase's `from agent.x import ...` / `from services.x import ...` style imports (they assume `backend/` itself is on `sys.path`, not the repo root), and separately collected `backend/test_ws.py` (a manual live-WebSocket script, not a pytest suite) and `backend/evals/test_golden_eval.py`, both of which fail outside their intended live-infra/nightly context. Fixed via `backend/pytest.ini` (`pythonpath = .`, anchoring `backend/` on `sys.path` regardless of invocation cwd) plus scoping CI's step to `pytest -q backend/tests` specifically, leaving `test_ws.py` and the eval suite uncollected by this job. Also discovered and worked around a real import-time fragility while wiring this up: merely importing `agent.response_types` runs `agent/__init__.py` → `core.py` → `customer_profile.py`, and unlike `core.py`'s deliberately lazy `get_agent()` (2026-07-18), `customer_profile.py` constructs a real `ChatOpenAI` client at **module import time**, which raises `OpenAIError` with no `OPENAI_API_KEY` present — exactly the case for the fast CI job, which has no OpenAI secret by design. `backend/tests/conftest.py` seeds `os.environ.setdefault("OPENAI_API_KEY", "test-key")` before any test module imports anything, mirroring the existing "mock-key" placeholder convention (`core.py`/`docker-compose.yml`) rather than granting this job real credentials it doesn't need — client *construction* doesn't hit the network, only an actual `.ainvoke()` would, and these tests never make one. Frontend unit tests were considered but explicitly out of scope per the user. |


## How to Run Locally

_(Placeholder — populate once `README.md` and `docker-compose.yml` exist. Target: a single `docker compose up` brings up Keycloak (pre-loaded realm/users), the backend, and the frontend.)_

## Notes for Future Claude Sessions

- Always check `progress-tracker.md` before starting work to see which subphase is active and what's already done.
- When a new architectural decision is made mid-implementation, add it to the decisions log above with the date, and update the Repo Layout section if the file/directory structure changed.
- Keep this file itself lightweight — deep rationale belongs in `project.md`, moment-to-moment task state belongs in `progress-tracker.md`. This file is for durable, project-specific instructions Claude needs every session.
